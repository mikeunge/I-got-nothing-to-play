{% extends 'base.html.twig' %}

{% block title %}Plattforms{% endblock %}

{% block body %}
    <h1>Plattforms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plattform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plattform.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plattforms_index') }}">back to list</a>

    <a href="{{ path('plattforms_edit', {'id': plattform.id}) }}">edit</a>

    {{ include('plattforms/_delete_form.html.twig') }}
{% endblock %}
