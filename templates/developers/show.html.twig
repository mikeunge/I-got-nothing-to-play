{% extends 'base.html.twig' %}

{% block title %}Developers{% endblock %}

{% block body %}
    <h1>Developers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ developer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ developer.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('developers_index') }}">back to list</a>

    <a href="{{ path('developers_edit', {'id': developer.id}) }}">edit</a>

    {{ include('developers/_delete_form.html.twig') }}
{% endblock %}
